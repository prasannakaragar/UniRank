# Ensure npm never skips optional dependencies (required for rollup native binaries on Linux/Vercel)
optional=true
# Use legacy peer deps to avoid workspace resolution conflicts
legacy-peer-deps=false
